import { ParralaxBackgroundDirective } from './parralax-background.directive';

describe('ParralaxBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ParralaxBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
